# Dashboard (Next.js) configuration
#
# NOTE: NEXT_PUBLIC_* variables are inlined into the browser bundle at BUILD TIME.
# If you build a Docker image, pass this as a build-arg (see docker-compose.yml).

# Browser -> Backend API base URL (used by client components)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Next.js server-side fetch base URL (internal Docker network name is best in compose)
API_BASE_URL=http://backend:5000

