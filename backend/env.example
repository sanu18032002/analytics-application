# Backend (Express) configuration
PORT=10000
MONGO_URI=mongodb://127.0.0.1:27017/user_analytics

# Comma-separated allowlist for CORS (e.g. "http://localhost:3000,http://localhost:8080")
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Safety limits
JSON_BODY_LIMIT=64kb

# Data retention (TTL) in seconds (default in code: 30 days)
EVENT_TTL_SECONDS=2592000

